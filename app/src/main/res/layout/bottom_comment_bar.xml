<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/bottom_comment_sheet"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_gravity="bottom"
    android:orientation="vertical"
    android:background="?attr/colorSurface"
    android:elevation="8dp"
    android:visibility="gone">

    <!-- Drag handle -->
    <View
        android:layout_width="40dp"
        android:layout_height="4dp"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="8dp"
        android:background="@drawable/comment_handle" />

    <!-- Reply context indicator (visible when replying) -->
    <RelativeLayout
        android:id="@+id/reply_context_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingHorizontal="16dp"
        android:paddingTop="12dp"
        android:visibility="gone">

        <TextView
            android:id="@+id/reply_context_tv"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Replying to @username"
            android:textSize="12sp"
            android:textColor="@color/secondaryColor"
            android:textStyle="bold"
            android:layout_alignParentStart="true"
            android:layout_centerVertical="true" />

        <ImageView
            android:id="@+id/cancel_reply_btn"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_alignParentEnd="true"
            android:src="@drawable/ic_baseline_cancel_24"
            android:padding="4dp"
            android:clickable="true"
            android:focusable="true"
            android:background="?selectableItemBackgroundBorderless"
            app:tint="@color/secondaryTextColor" />
    </RelativeLayout>

    <!-- Comment input area -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingHorizontal="16dp"
        android:paddingTop="8dp"
        android:paddingBottom="16dp">

        <!-- User avatar -->
        <androidx.cardview.widget.CardView
            android:id="@+id/comment_user_avatar_cv"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_alignParentStart="true"
            android:layout_centerVertical="true"
            app:cardCornerRadius="20dp"
            app:cardElevation="2dp"
            app:cardBackgroundColor="@color/avatar_background">

            <ImageView
                android:id="@+id/comment_user_avatar_iv"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="centerCrop"
                android:src="@drawable/user_solid_full" />
        </androidx.cardview.widget.CardView>

        <!-- Input field with reply hint -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_toEndOf="@id/comment_user_avatar_cv"
            android:layout_marginStart="12dp"
            android:orientation="vertical">

            <EditText
                android:id="@+id/comment_et"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:minHeight="48dp"
                android:background="@null"
                android:hint="Add a comment..."
                android:textSize="14sp"
                android:textColor="?android:textColorPrimary"
                android:textColorHint="@color/secondaryTextColor"
                android:maxLength="500"
                android:maxLines="4"
                android:paddingVertical="12dp"
                android:paddingEnd="56dp"
                android:inputType="textCapSentences|textMultiLine" />

            <TextView
                android:id="@+id/comment_username_hint"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Commenting as username"
                android:textSize="11sp"
                android:textColor="@color/secondaryTextColor"
                android:layout_marginTop="2dp" />
        </LinearLayout>

        <!-- Submit button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/comment_submit_btn"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:layout_marginEnd="8dp"
            android:enabled="false"
            android:insetTop="0dp"
            android:insetBottom="0dp"
            android:padding="0dp"
            app:backgroundTint="@color/secondaryColor"
            app:icon="@drawable/baseline_send_24"
            app:iconTint="@color/white"
            app:iconSize="20dp"
            app:iconGravity="textStart"
            app:iconPadding="0dp"
            app:rippleColor="@color/secondaryColor_20"
            app:cornerRadius="24dp"
            app:elevation="2dp"
            android:stateListAnimator="@null"
            style="@style/Widget.MaterialComponents.Button" />
    </RelativeLayout>
</LinearLayout>
<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" android:required="false" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"  android:required="false"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" android:required="false" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" android:required="false" tools:node="remove" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" android:required="false"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />
    <application
        android:launchMode="singleInstance"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/Theme.StoryPop"
        android:usesCleartextTraffic="${usesCleartextTraffic}"
        >
        <activity
            android:name=".LauncherActivity"
            android:exported="true">
            <tools:validation testUrl="https://icpress.org/account/verify?8ioo3i2nfnlreg" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https"/>
                <data android:host="icpress.org"/>
                <data android:path="/account/verify"/>
                <data android:path="/wallet/verify"/>
                <data android:pathPattern="/s/..*"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".ArticleContentActivity"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.StoryPop.NoActionBar" />
        <activity
            android:name=".ArticleListActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.StoryPop.NoActionBar">
        </activity>
        <activity
            android:name=".ArticleContentEditActivity"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.StoryPop.NoActionBar" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity
            android:name=".ProfileActivity"
            android:theme="@style/Theme.StoryPop.NoActionBar" />
        <activity
            android:name=".AIWizardActivity"
            android:theme="@style/Theme.StoryPop.NoActionBar" />
        <activity
            android:name=".ArticleReviewActivity"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.StoryPop.NoActionBar" />

        <activity
            android:name=".map.editor.MapEditorActivity"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.StoryPop.NoActionBar" />

        <activity
            android:name=".FullscreenMapActivity"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.StoryPop.NoActionBar" />
        <service
            android:name=".CloudMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <!-- make sure within the application tag, otherwise app will crash with XmlResourceParser errors -->

        <provider

            android:name="androidx.core.content.FileProvider"

            android:authorities="net.crowdventures.storypop.Fileprovider"

            android:exported="false"

            android:grantUriPermissions="true">

            <meta-data

                android:name="android.support.FILE_PROVIDER_PATHS"

                android:resource="@xml/fileprovider" />

        </provider>
    </application>
    <queries>
        <!-- Camera -->
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
        <package android:name="app.phantom" />
    </queries>
</manifest>